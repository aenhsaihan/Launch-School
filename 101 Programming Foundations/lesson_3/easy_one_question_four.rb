numbers = [1, 2, 3, 4, 5]

numbers.delete_at(1)
numbers.delete(1)

# numbers.delete_at(1) would delete the element at the second index and return it
# numbers.delete(1) would delete 1 from the collection and return it